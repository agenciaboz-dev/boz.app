import React from "react"
import { Box } from "@mui/material"

interface HeaderProps {}

export const Header: React.FC<HeaderProps> = ({}) => {
    return <Box sx={{ height: "10vh", width: "100vw" }}></Box>
}
