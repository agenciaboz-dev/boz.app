import React from 'react'
import {Box} from '@mui/material'

interface LoginProps {
    
}

export const Login:React.FC<LoginProps> = ({  }) => {
    
    return (
        <Box sx={{backgroundColor: 'primary.main', width: '100vw', height: '100vh'}}>
            
        </Box>
    )
}