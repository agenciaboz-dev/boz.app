import React from "react"
import { Box } from "@mui/material"

interface SocketContainerProps {}

export const SocketContainer: React.FC<SocketContainerProps> = ({}) => {
    return <Box sx={{}}></Box>
}
